{
  "name": "rnd-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "RND monorepo — rnd-ui package + portfolio app",
  "author": "Rindho Ananta Samat",
  "license": "ISC",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm -w portfolio run dev",
    "build": "npm -w rnd-ui run build && npm -w portfolio run build",
    "build:ui": "npm -w rnd-ui run build",
    "build:portfolio": "npm -w portfolio run build",
    "lint": "npm -ws run lint",
    "lint:fix": "npm -ws run lint:fix"
  }
}